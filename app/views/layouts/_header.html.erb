<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom border-grey">
    <div class="container">
        <%= link_to image_tag("logo.png", style: 'height:35px;width:auto;'), '/', class: 'navbar-brand' %>
        <div class = "search">
           <%= search_form_for @search, url:search_path, method: :get do |f| %>
                <%= f.text_field :address_cont, placeholder: "Where are you going?", class: "form-control" %>
            <% end %>
        </div>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02"
            aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ml-auto "> 
                    <li class="nav-item">
                    <%= link_to "Become a host", new_room_path , class: "btn btn-outline-success" %>
                    </%=>  
                <% if user_signed_in? %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <% if current_user.image %>
                                <%= image_tag current_user.image, class: "rounded-circle", style: 'height:25px;width:auto;' %>
                            <% else %>
                                <%= image_tag gravatar_for(current_user), class: "rounded-circle", style: 'height:25px;width:auto;' %>
                            <% end %>
                            <%= current_user.fullname %>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <%= link_to "Manage Listing", rooms_path, class:"dropdown-item" %>
                            <%= link_to "List Your Space", new_room_path, class:"dropdown-item" %>
                            <%= link_to "Your Reservations", your_reservations_path, class:"dropdown-item" %>
                            <%= link_to "Your Trips", your_trips_path, class:"dropdown-item" %>
                        <div class="dropdown-divider"></div>
                            <%= link_to "Edit Profile", edit_user_registration_path, class:"dropdown-item" %>    
                            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                        </div>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= link_to "Login ", new_user_session_path, class: "nav-link" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Sign Up', new_user_registration_path, class: "nav-link" %>
                    </li>   
                <% end %>
            </ul>
        </div>
    </div>
</nav> 